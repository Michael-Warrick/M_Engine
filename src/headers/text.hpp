#include "definitions.hpp"

/*
#include <ft2build.h>
#include FT_FREETYPE_H

class UIText
{
public:
    void LoadFont(const GLchar* font, const GLchar* vertShaderPath, const GLchar* fragShaderPath);
    void CompileFontShader();
    void RenderText(string text, float x, float y, float scale, vec3 color);

    FT_Library freetypeLibrary;
    FT_Face fontFace;

    string fontCode;
    string vertShaderCode;
    string fragShaderCode;

    GLuint someShit;

    GLuint fontShaderProgram;

    GLuint UITextVAO;
    GLuint UITextVBO;
};
*/